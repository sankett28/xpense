// Single source of truth for app-wide constants.
export const APP_NAME = "X-PENSE";

// "Available / left" for the current cycle = total credited (salary + others)
// minus spend in the cycle. Flip to true to base "available" on salary only.
export const AVAILABLE_FROM_SALARY_ONLY = false;
